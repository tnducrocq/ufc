<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="RoundedImageView">
        <attr name="radius" format="float"/>
    </declare-styleable>

    <declare-styleable name="RatioImageView">
        <attr name="image_ratio" format="float"/>
    </declare-styleable>

    <declare-styleable name="Pinnable">
        <attr name="state_pinned" format="boolean"/>
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference"/>
        <attr name="metaButtonBarButtonStyle" format="reference"/>
    </declare-styleable>

    <declare-styleable name="PieChart">
        <attr name="color" format="color"/>
        <attr name="test" format="float"/>
    </declare-styleable>

    <declare-styleable name="CardPieView">
        <attr name="remaining" format="string"/>
        <attr name="title" format="string"/>
        <attr name="lineWidth" format="dimension"/>

        <attr name="percentageAppearance" format="reference"/>
        <attr name="remainingAppearance" format="reference"/>
        <attr name="titleAppearance" format="reference"/>
    </declare-styleable>

</resources>